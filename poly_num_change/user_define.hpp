#pragma once

//////// you need to set ////////
double new_pori = 2.5;
double old_pori = 1.5;

double m_mul = 10.0;
double r_mul = 4.0;
/////////////////////////////////

double new_ganma = 1 + (1 / new_pori);
double old_ganma = 1 + (1 / old_pori);
